services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - 6333:6333
    environment:
      - QDRANT_STORAGE_TYPE=disk
      - QDRANT_STORAGE_PATH=/data
    volumes:
      - qdrant_data:/data

volumes:
  qdrant_data:
    driver: local 